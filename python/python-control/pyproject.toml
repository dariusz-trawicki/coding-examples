[project]
name = "pid-control-simulation"
version = "0.1.0"
description = "PID controller example with python-control"
requires-python = ">=3.9"

dependencies = [
  "numpy>=1.24",
  "matplotlib>=3.7",
  "control>=0.9.4"
]
