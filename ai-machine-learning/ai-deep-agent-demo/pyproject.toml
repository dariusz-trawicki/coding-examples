[project]
name = "deep-agent-demo"
version = "0.1.0"
description = "Minimal deep agent demo (Anthropic API + Tavily)"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40.0",
    "requests>=2.32.0",
    "python-dotenv>=1.0.0",
]
